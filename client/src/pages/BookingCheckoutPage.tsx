import React from 'react';
import { BookingStepFlow } from '../components/booking/BookingStepFlow';

export const BookingCheckoutPage: React.FC = () => {
  return (
    <div className="py-6">
      <BookingStepFlow />
    </div>
  );
};
